# Finalization state

Current source base: exact `qr-resets-5.zip` / accepted V5 source tree.

Current release behavior:

- ResourceCite is the default entry product; QR Resets remains manually accessible as a sibling prototype view.
- New visitors default to the light theme; the cool-blue dark theme remains selectable.
- Public Directory branding is ResourceCite.
- ResourceCite public cards/categories/search/detail/compare/Crisis Navigator have the post-deployment cleanup and mobile corrections applied.
- Public machine-facing metadata is removed from ordinary ResourceCite presentation while internal tags, provenance, evidence, verification, and audit data are retained.
- ResourceCite Site Copy is a controlled admin editor with draft, preview, revision history, publish, and restore.
- Individual permanent resource deletion captures and verifies a full pre-delete snapshot and exposes hash-verified exact undo.
- Cleanup and verification material actions use explicit consequence confirmations; existing snapshot/restore protections remain intact.
- QR Resets request persistence, payments/donations, donation webhooks, case funding, and operational QR mutation remain structurally disabled.
- The bundled verifier-v4 pass over the 559-record master input produced 545 strong-deduped candidates. Restricted network access prevented live verification, so no uncertain candidate was bulk-published; canonical data remains 114 rows with the original dataset hash.
- Dependency-free package, dataset, import-graph, syntax, data-safety, and contract gates pass. Clean dependency installation/build/browser gates remain environment-dependent and must never be relabeled PASS when unavailable.
